/*#include<iostream>
#include<cmath>
using namespace std;
int main(int argc, char const *argv[])
{
	//implements of quadratic equation:
	double a,b,c;
	cout << "Enter the Coefficients:" << endl;
	cout << "\ta:";
	cin >> a;
	cout << "\tb:";
	cin >> b;
	cout << "\tc:";
	cin >> c;
	cout << "The Equation is:"<< a << "*X*X + " << b << "*X + " << c << " = 0 " << endl;
	double d = (b*b-4*a*c);
	double sqrtd = sqrt(d);
	double X1 = (-b+sqrtd)/(2*a);
	double X2 = (-b-sqrtd)/(2*a);
	cout << "The Solutions are:" << endl;
	cout << "\tX1 = " << X1 << endl;
	cout << "\tX2 = " << X2 << endl;
	cout << "Check:" << endl;
	cout << "\ta*X1*X1 + b*X1 + c = " << a*X1*X1 + b*X1 + c << endl;
	cout << "\ta*X2*x2 + b*X2 + c = " << a*X2*X2 + b*X2 + c << endl;
	return 0;
}


*/

#/*include<iostream>
using namespace std;
int main(int argc, char const *argv[])
{
	int t;
	cin >> t;
	while(t--){
		int n, a, b, c, d;
		cin >> n >> a >> b >> c >> d;
		if (((a+b)*n < c-d) || ((a-b)*n > c+d))
		{
			cout << "NO" << endl;
		}
		else cout << "YES" << endl;
	}
	return 0;
}*/

